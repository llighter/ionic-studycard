export class CardDTO {
    question: string;
    answer: string;
    source: string;
    failCount: number;
    stage: number;

    // TODO: add additional member
    // resource: string;
    // tag: string[];
}